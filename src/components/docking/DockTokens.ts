/**
 * D51.2 — Dock token bridge (reference-only).
 * Values live exclusively in UI_TOKENS.dock — no numeric/string/class literals here.
 */
import { UI_TOKENS } from "@/lib/ui/tokens";

export const DOCK_TOKENS = UI_TOKENS.dock;
