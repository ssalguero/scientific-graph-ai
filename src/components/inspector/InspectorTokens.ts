/**
 * D50.2 — Inspector token bridge (reference-only).
 * Values live exclusively in UI_TOKENS.inspector — no string/class/CSS literals here.
 */
import { UI_TOKENS } from "@/lib/ui/tokens";

export const INSPECTOR_TOKENS = UI_TOKENS.inspector;
