/**
 * PLUGINS reserved-layer status helper (I0).
 * Status markers only — no runtime services.
 */

export type PluginsReservedStatus = "RESERVED";

export const PLUGINS_RESERVED_STATUS = "RESERVED" as const;
