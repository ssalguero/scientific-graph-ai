/**
 * Scientific Model Layer (DATA-P2).
 *
 * Conceptual scientific representation. Contains no processing logic.
 * DATA-I2: Scientific Model Manager Authoritative Registry is active
 * under `scientific-model-manager/` (identity SSOT only).
 *
 * @packageDocumentation
 */

export { ScientificModelManager } from "./scientific-model-manager";
