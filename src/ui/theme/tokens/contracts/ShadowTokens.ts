/**
 * UX-3.2.1 — Resolved shadow token contract.
 * Typed projection of Foundation primitive shadow scale (leaves: string).
 */

export interface ShadowTokens {
  readonly none: string;
  readonly sm: string;
  readonly md: string;
  readonly lg: string;
  readonly xl: string;
}
