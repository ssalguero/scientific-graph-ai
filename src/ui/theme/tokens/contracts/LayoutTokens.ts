/**
 * UX-3.2.1 — Layout token contract (placeholder).
 *
 * Foundation does not yet define a layout token domain.
 * This interface is an extension point only — no regions, sizes, or constants.
 */

export interface LayoutTokens {}
