/**
 * UX-3.2.1 — Resolved radius token contract.
 * Typed projection of Foundation SemanticRadiusTokens (leaves: string, not TokenRef).
 */

export interface RadiusTokens {
  readonly control: string;
  readonly container: string;
  readonly pill: string;
}
