/**
 * UX-7.5 — Context Help Foundation types.
 * Identity = VisibilityId (no parallel ContextHelpId brand).
 * VisibilityId Freeze — exact VisibilityId; no convert / rebrand / align.
 */

export type { VisibilityId } from "../visibility/VisibilityTypes";
