/**
 * UX-6.5 — Command Palette shared types.
 *
 * Command identity remains CommandId from the Commands SSOT.
 * No parallel branded command ids · no keywords model · no React.
 */

/** Trimmed query text accepted by structural palette search. */
export type CommandPaletteQueryText = string;
