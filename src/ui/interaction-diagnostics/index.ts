/**
 * UX-8.8 — Interaction Diagnostics local barrel.
 * Not re-exported from @/ui (src/ui/index.ts) in this phase.
 * API Freeze: exactly InteractionDiagnosticsReport + createInteractionDiagnosticsReport.
 */

export type { InteractionDiagnosticsReport } from "./InteractionDiagnosticsReport";
export { createInteractionDiagnosticsReport } from "./createInteractionDiagnosticsReport";
